# 怎么更换模型？

## 操作方式

在你的代码或软件客户端的 **Model / 模型名称** 字段中，直接填写目标模型的名称即可。

!!! warning "更换前请确认"
    确保你想使用的模型已包含在当前令牌所选的 **分组** 中。不同分组支持的模型范围不同。

!!! tip "获取正确模型名称"
    建议直接在 **「模型广场」** 复制模型名称，避免手动输入出错。模型名称必须完全一致，例如：
    ```
    claude-sonnet-4-5-20250929
    ```
