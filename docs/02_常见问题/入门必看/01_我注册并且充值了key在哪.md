# 我注册并且充值了，Key 在哪？

!!! success "找到 Key 的步骤"
    注册登录后，进入 **工作台 → 令牌** 页面，即可看到你创建的令牌列表。

每一行令牌右侧都有一个 **复制按钮**，点击复制即可获取密钥 Key。

获取 Key 后，在你的代码或客户端中配置以下两项即可与模型交互：

| 配置项 | 说明 |
|--------|------|
| **Key（密钥）** | 复制的令牌字符串，格式为 `SK-xxx...` |
| **接口地址（Base URL）** | 平台提供的 API 地址 |
