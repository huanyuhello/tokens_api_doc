# 2. 修改个人信息

用户名（≤20字符）

display_namestring 必需显示名称（≤20字符）

original_passwordstring 必需新密码（8-20字符）

passwordstring 原密码必需sidebar_modulesstring 更新侧边栏模块必需languagestring 更新语言偏好必需示例
## 返回响应
🟢200成功application/json生成代码Body生成代码object 请求示例请求示例ShellJavaScriptJavacURLcURL-WindowsHttpiewgetPowerShell
```
curl --location --request PUT 'https://tokens.smartfashionai.cn//api/user/self' \
--header 'Authorization: token' \
--header 'New-Api-User: 126638' \
--header 'Content-Type: application/json' \
--data-raw '{
    "username": " name",
    "display_name": "name",
    "original_password": "old pass",
    "password": "new pass"
}'
```
响应示例响应示例
```
{}
```