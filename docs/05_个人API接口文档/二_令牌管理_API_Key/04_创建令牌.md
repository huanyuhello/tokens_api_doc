# 4.创建令牌

系统访问令牌（个人设置中生成）

示例:{access_token}New-Api-Userstring 用户 ID（数字）可选示例:{user_id}Body 参数application/json必填生成代码namestring 名称（≤50字符）必需expired_timeinteger 可选过期时间戳（-1=永不过期）

remain_quotainteger 可选额度（非无限时，0 ~ 10亿×QuotaPerUnit）

unlimited_quotaboolean 是否无限额度可选model_limits_enabledboolean 是否启用模型限制可选model_limitsstring 可选可用模型（逗号分隔）

allow_ipsstring 可选IP 白名单（换行分隔）

groupstring 分组可选cross_group_retryboolean 跨分组重试可选示例
## 返回响应
🟢200成功application/json生成代码Body生成代码object 请求示例请求示例ShellJavaScriptJavacURLcURL-WindowsHttpiewgetPowerShell
```
curl --location --request POST 'http://47.102.134.41:3000/api/token/' \
--header 'Authorization: {access_token}' \
--header 'New-Api-User: {user_id}' \
--header 'Content-Type: application/json' \
--data-raw '{
    "name": "Tokens",
    "expired_time": 1769280706264,
    "remain_quota": 18980,
    "unlimited_quota": false,
    "model_limits_enabled": true,
    "model_limits": "gpt-5.4",
    "group": "Gpt-codex",
    "cross_group_retry": false
}'
```
响应示例响应示例
```
{
    "message": "",
    "success": true
}
```