# 批量查询

原生接口
批量查询
**POST** `https://4sapi.com/mj/task/list-by-condition`

## 请求参数

### Header 参数

| 参数名 | 类型 | 必填 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `Accept` | string | 必需 | Authorization |  |
| `Content-Type` | string | 必需 | application/json |  |

### Body 参数

## 返回响应

## 请求示例

```bash
curl --location --request POST 'https://4sapi.com/mj/task/list-by-condition' \
--header 'Accept: application/json' \
--header 'Authorization: sk-' \
--header 'Content-Type: application/json' \
--data-raw '{
    "ids": [
        "1749688282741136"
    ]
}'
```

## 响应示例

```json
{}
```